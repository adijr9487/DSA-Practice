//g++ -std=c++11 quick.cpp -o quick && ./quick
#include<iostream>
#include<bits/stdc++.h>

using namespace std;

// vector<int> quickSort(vector<int>& vm, ){

// }


int main()
{
    map<int, int> death;
    // vector<int> v = {4, 5, 6, 1, 2, 5, 2, 3, 5, 3};
    for (int a = 0, b = 0; a < 10 && b < 4; a++, b++)
    {
        cout << a << " " << b << "\n";
    }
    return 0;
}